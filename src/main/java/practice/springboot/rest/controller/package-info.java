/**
 * Contains all controllers used in the application. Separate controllers should be created for similar APIs,
 * rather than keeping all APIs in one controller. For example:
 * <ul>
 *     <li>EmployeeController to expose all employee related APIs</li>
 *     <li>OrganizationController to expose all employee related APIs</li>
 * </ul>
 * and so on.
 */
package practice.springboot.rest.controller;